# Optional non-secret overrides only. Secret material stays in ignored local files.
FEEDBACK_PORT=8080
ADMIN_USERNAME=admin
# The secret file defaults are documented in README.md; keep them out of Git.

# Keep false behind an HTTP reverse proxy. Set true when served via HTTPS.
COOKIE_SECURE=false
